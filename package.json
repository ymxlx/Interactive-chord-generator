{
  "name": "interactive-chord-generator",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "packages/chord-core"
  ],
  "scripts": {
    "dev": "npm run dev --workspace interactive-chord-generator-web",
    "build": "npm run build --workspace chord-engine-core && npm run build --workspace interactive-chord-generator-web",
    "test": "npm run test --workspace interactive-chord-generator-web"
  }
}
